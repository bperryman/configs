# Setup for emacs

```sh
mkdir -p ${HOME}/elisp/paredit
curl -l http://mumble.net/~campbell/emacs/paredit.el -o ${HOME}/elisp/paredit/paredit.el
git clone https://github.com/slime/slime.git ${HOME}/elisp/slime
```
